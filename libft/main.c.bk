#include <unistd.h>
int	ft_isalpha(char c);

int	main(void)
{
	char c;

	c = '1';
	if (ft_isalpha(c) == 0)
		write(1, "no", 2);
	else if (ft_isalpha(c) == 1)
		write(1, "si", 2);
	else
		return (0);
}
